spring.application.name=java_assignment

# ========================
# SERVER CONFIG
# ========================
server.port=8080

# ========================
# DATABASE CONFIG
# ========================
spring.datasource.url=jdbc:postgresql://localhost:5432/java-assignment
spring.datasource.username= postgres
spring.datasource.password= rafsan1234
spring.datasource.driver-class-name=org.postgresql.Driver

# ========================
# JPA / HIBERNATE CONFIG
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ========================
# JWT CONFIG
# ========================
jwt.secret=yourSuperSecretKeyForJWT

# ========================
# SWAGGER UI CONFIG (Springdoc)
# ========================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

logging.level.com.zaxxer.hikari=DEBUG
